{###############################################################################

Component: Section
===============

TODO
----

- [ ]

###############################################################################}

{#% from 'components/card/card__block.njk' import cardBlock %#}

{# START MACRO

Arguments
---------
- body          : * (string, other component, etc)
- accent        : boolean (if true, will use the site's accent color as a background)
- bgURL         : string

TODO:
- imageURL
- bg-type
- overlay-type

#}

{% macro section(
    body,
    accent = false
  ) %}

  {# START COMPONENT #}
  <section class="section{{ " section__accent" if accent }}">
    <div class="container">
      {{ body }}
    </div>
  </section>
  {# /COMPONENT #}

{% endmacro %}
{# /MACRO #}
