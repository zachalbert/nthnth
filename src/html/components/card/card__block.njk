{#
  Card / Card Block:
  - bg: hex value (default: "green")
#}

{% macro cardBlock(
    accent = false,
    body
  ) %}
  <div class="card__block{{ " card__block--accent" if accent }}">
    {{ body | safe }}
  </div>
{% endmacro %}
